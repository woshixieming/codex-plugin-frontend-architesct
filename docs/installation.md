# Installation

Copy the `frontend-architect-os` folder to:

```text
~/.codex/plugins/frontend-architect-os/
```

Recommended command:

```bash
mkdir -p ~/.codex/plugins
cp -R frontend-architect-os ~/.codex/plugins/
```

Restart Codex Desktop if the plugin is not detected immediately.
